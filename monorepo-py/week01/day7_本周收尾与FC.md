# 第 1 周第 7 天：本周收尾与 Function Calling 小结（模板）

> 复制到个人笔记后填写即可；**勿**把真实业务数据提交到公共仓库。

## 1. 补全 vs 多轮（各举一个虚构业务例子）

| 类型 | 你的例子 |
|------|----------|
| **补全** | （例：IDE 单行续写，只发当前光标上下文，无会话 id） |
| **多轮** | （例：客服需带历史 5 轮，否则指代消解失败） |

## 2. FC 小结（≤5 行：第二次请求前 `messages` 里多了什么）

1.  
2.  
3.  
4.  
5.  

（参照周计划第 7 天正文中的「FC 小结」五点，用自己的话写。运行 `python scripts/fc_min.py` 时可在调试器里观察 `messages` 列表。）

## 3. 可选：本周 tag

```bash
git tag -a week01 -m "第1周收尾"
```
