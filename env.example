# 本仓库根目录的「环境变量速查」模板：无真实密钥，可安全提交。
# 实际使用：
#   - Python：在 monorepo-py 下复制为 .env（见 monorepo-py/.env.example）
#   - Java：在启动 java-demo 的终端里 export，或写进本机 shell 配置（勿提交）
#
# 通义 / DashScope（与当前 health 脚本、java-demo 一致）
# DASHSCOPE_API_KEY=        # monorepo-py 的 health_chat 优先读
# AI_DASHSCOPE_API_KEY=     # Spring 官方文档常用名；未设时 java-demo 会回读 DASHSCOPE_API_KEY
# LLM_MODEL=qwen-turbo      # 可选；通义下 FC（fc_min.py、/api/v1/fc/chat）建议 qwen-plus；java-demo 的 model 会读此变量（见 application.yml）
# APP_EXPECTED_API_KEY=     # 可选；java-demo 第 1 周第 5 天起：覆盖占位 API Key（默认 demo，对应请求头 X-API-Key）
#
# 若使用 OpenAI 兼容其它网关，见 monorepo-py/.env.example 中的 OPENAI_* 说明。
